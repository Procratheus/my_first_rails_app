<%= form_for [post, comment] do |f|%>

  <div class="form-group">
    <%= f.label :body %>
    <%= f.text_area :body, class: "form-control", row: 8, placeholder: "Enter comment body."  %> 
  </div>
  <div class="form-group">
    <%= f.submit "save", class: "btn btn-success" %>
  </div>
  
<% end %>